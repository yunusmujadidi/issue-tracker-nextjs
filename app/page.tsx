import React from "react";
import Pagination from "./components/Pagination";

interface Props {
  searchParams: {
    page: string;
  };
}
const Home = ({ searchParams }: Props) => {
  return <></>;
};

export default Home;
